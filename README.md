# singham-web

This repository contains a minimal static website that can be deployed to DigitalOcean App Platform with a single
pull‑request or `doctl` command.

## Deploy

```bash
doctl apps create --spec app.yaml
